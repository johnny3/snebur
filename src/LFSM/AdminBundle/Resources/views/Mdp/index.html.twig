{% extends '::base.html.twig' %}

{% block body %}
<div class="container">
    <h3>Gestion des mode de paiement</h3>
    
    <div class="col-tableau">
        <div class="new_item_admin">
            <a href="{{ path('mdp_new') }}">
                <img src="{{ asset('bundles/lfsmdonateur/images/new_item.png') }}" border="0" onmouseover="montre('Nouveau');" onmouseout="cache();">
            </a>
        </div>
        <div class="pagination_navigation">
            <div class="pagination_custom">
                <p>Page {{pagination.getCurrentPageNumber}}/{{totalPages}} ({{pagination.getTotalItemCount}} entrées)</p>
            </div>
            <div class="navigation">
                {{ knp_pagination_render(pagination) }}
            </div>
        </div>
        <table width="100%">
            <tbody>
                <tr>
                    <th width="15%">{{ knp_pagination_sortable(pagination, 'Libellé', 'q.mdp_lib') }}</th>
                    <th width="15%">Action</th>
                </tr>
                {% for entity in pagination %}
                <tr class="fdblanc">
                    <td>{{ entity.mdplib }}</td>
                    <td><a href="{{ path('mdp_edit', { 'id': entity.id }) }}">Modifier</a></td>
                </tr>
                {% endfor %}
            </tbody>
        </table>    
    </div>
    <div class="clear"></div>
</div>
{% endblock %}