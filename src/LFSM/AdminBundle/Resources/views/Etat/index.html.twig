{% extends '::base.html.twig' %}

{% block body %}
<div class="container">
    <h3>Gestion des états</h3>
    
    <div class="col-tableau">
        <div class="new_item_admin">
            <a href="{{ path('etat_new') }}">
                <img src="{{ asset('bundles/lfsmdonateur/images/new_item.png') }}" border="0" onmouseover="montre('Nouveau');" onmouseout="cache();">
            </a>
        </div>
        <div class="pagination_navigation">
            <div class="pagination_custom">
                <p>Page {{pagination.getCurrentPageNumber}}/{{totalPages}} ({{pagination.getTotalItemCount}} entrées)</p>
            </div>
            <div class="navigation">
                {{ knp_pagination_render(pagination) }}
            </div>
        </div>
        <table width="100%">
            <tbody>
                <tr>
                    <th width="15%">{{ knp_pagination_sortable(pagination, 'Libellé', 'q.etat_lib') }}</th>
                    <th width="15%">Action</th>
                </tr>
                {% for entity in pagination %}
                <tr class="fdblanc">
                    <td>{{ entity.etatlib }}</td>
                    <td><a  class="modif" href="{{ path('etat_edit', { 'id': entity.id }) }}">Modifier</a></td>
                </tr>
                {% endfor %}
            </tbody>
        </table>    
    </div>
    <div class="clear"></div>
</div>
{% endblock %}