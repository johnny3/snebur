{% extends '::base.html.twig' %}

{% block body %}
<div class="container">
    <h3 class="new_item_h3">Mise à jour de la fonction</h3>

    <div class="background_new_item">

        <form action="{{ path('fonction_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
            <div class="col_form_new_edit">
                <table>
                    <tr>
                        <td>{{ form_label(edit_form.fct_lib, 'Libellé') }}</td>
                        <td>{{ form_widget(edit_form.fct_lib) }}</td>        
                    </tr>
                </table>
            </div>
                {{ form_rest(edit_form) }}
                {{ form_errors(edit_form) }}    

            <div class="buttons_action">
                <button type="submit" class="save">&nbsp;</button>
                <a href="{{ path('fonction_delete', { 'id': entity.id }) }}" class="delete_link"><img src="{{ asset('bundles/lfsmdonateur/images/Delete.gif') }}"></a>
            </div>
        </form>
    </div>
    
    <div class="clear"></div>
</div>
{% endblock %}
