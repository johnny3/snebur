<h1>RefProvenanceFichier list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Prov_lib</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('refprovenancefichier_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.provlib }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('refprovenancefichier_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('refprovenancefichier_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('refprovenancefichier_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
