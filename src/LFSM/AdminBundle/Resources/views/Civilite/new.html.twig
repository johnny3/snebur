{% extends '::base.html.twig' %}

{% block body %}
<div class="container">
    <h3 class="new_item_h3">Nouvelle civilité</h3>

    <div class="background_new_item">

        <form action="{{ path('civilite_create') }}" method="post" {{ form_enctype(form) }}>
            <div class="col_form_new_edit">
                <table>
                    <tr>
                        <td>{{ form_label(form.civ_lib_court, 'Libellé court') }}</td>
                        <td>{{ form_widget(form.civ_lib_court) }}</td>        
                    </tr>

                    <tr>
                        <td>{{ form_label(form.civ_lib, 'Libellé') }}</td>
                        <td>{{ form_widget(form.civ_lib) }}</td>        
                    </tr>
                </table>
            </div>
                {{ form_rest(form) }}
                {{ form_errors(form) }}    

            <div class="buttons_action">
                <button type="submit" class="save">&nbsp;</button>
            </div>
        </form>
    </div>
    <div class="clear"></div>
</div>
{% endblock %}